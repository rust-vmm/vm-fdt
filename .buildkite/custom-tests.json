{
  "tests": [
    {
      "test_name": "build-alloc",
      "command": "RUSTFLAGS=\"-D warnings\" cargo build --release --target {target_platform}-unknown-none --no-default-features --features alloc",
      "platform": [
        "x86_64",
        "aarch64"
      ]
    }
  ]
}
